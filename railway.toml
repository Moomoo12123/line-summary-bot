[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -c 'from app.database import init_db; init_db()' && gunicorn app.main:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
